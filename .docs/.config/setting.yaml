commands:
  - name: "notepad"
    program: "notepad.exe"
    args: []

  - name: "open-home"
    program: "explorer.exe"
    args:
      - "."

  - name: "open-url"
    program: "cmd.exe"
    args:
      - "/c"
      - "start"
      - ""
      - "https://example.com"

  - name: "env-test"
    program: "$program"
    args:
      - "$arg1"
      - "$arg2"
      - "$arg1"
      - "$arg3"