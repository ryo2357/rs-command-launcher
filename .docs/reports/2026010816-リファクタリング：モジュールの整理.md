# 実装報告書: リファクタリング：モジュールの整理

- 対象コミット
  - fdfadeb5023ea66669e3cbd943bae2761067ee6b
  - リファクタリング：モジュールの整理

## 目的

- UI 起動処理と初期化処理の配置を整理し、モジュール名を一貫させる

## 実装内容

- UI モジュールの整理

  - src/ui/launcher.rs を src/ui/app.rs にリネーム
  - src/ui/initialize.rs を src/ui/startup.rs にリネームし、UI 起動処理を集約
  - src/ui/runnner.rs を削除し、UI 起動処理を src/ui/startup.rs に統合
  - src/ui/mod.rs の公開モジュール定義を app/hotkey/startup に更新

- UI 起動経路の更新

  - src/main.rs の引数なし起動時の UI 起動先を ui::startup::startup に変更

## 仕様書への反映

- .docs/specs/project.md

  - UI モジュール構成のファイル名を app/startup に更新
  - 現時点の実装範囲の参照先を src/ui/app.rs に更新

- .docs/specs/history.md

  - 本コミットの変更内容を仕様書変更履歴に追記

## 影響範囲と注意点

- 動作仕様の追加や変更はなく、主にファイル配置と呼び出し経路の整理
- ui::startup::startup が UI 起動のエントリーポイントとなる

## 動作確認

- ビルド
  - cargo build
- 実行
  - cargo run
